
const { Schema, model } = require("mongoose");

// Create data for Week model
/*
--> Which data does the Week model need?

const weekSchema = new Schema(
  {
    username: {
      type: String,
      required: false,
      unique: true,
      trim: true,
    },
  }
);


const Week = model("Week", weekSchema);

module.exports = Week;

*/